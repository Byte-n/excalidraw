# 第 08 期：文本格式导入导出

- 期次：P08。
- 状态：待实施。
- 里程碑：Markdown、Outline、OPML 互通闭环。
- 总体约束与完成门槛：[分期总览](00-mindmap-overview.md)。

## 依赖关系

- **前置验收**：[P07 搜索与大纲侧栏](07-mindmap-search-and-outline.md) 须先通过用户界面审查。
- **主要能力依赖**：[P04](04-mindmap-copy-and-integration.md) 的身份重建、独立副本生成与完整数据收集；P07 的层级展示。
- **外部依赖**：无新增服务，文件与粘贴输入在应用内解析；协作回归沿用 P06。
- **后续承接**：当前进入 [P10 移动端与大图性能](10-mindmap-mobile-and-performance.md)；P11 复用结构化树输入校验链路。P09 模板与主题暂缓。

## 用户可见目标

用户可以把文本或大纲文件直接创建为独立的新导图，并把现有导图导出为三种层级文本格式。

## 实施任务

### 格式解析与错误反馈

- [ ] 提供文件导入和粘贴文本入口，支持 Markdown、Outline、OPML。
- [ ] Markdown 支持标题与嵌套列表的层级表达；Outline 支持缩进层级；OPML 支持树结构。
- [ ] 将各格式解析为统一的树描述，先校验层级、文字、顺序和规模，通过后一次性写入 Scene。
- [ ] 明确提示不支持或不能无损表达的内容，不能静默丢弃后再宣称完整导入。
- [ ] 对格式错误、非法层级和规模超限展示可定位的错误信息。
- [ ] 导入内容按文本处理，不执行 HTML、脚本或外部资源解析；OPML 不解析外部实体。

### 导入提交

- [ ] 导入直接创建独立新导图，生成新的 `graphId`、节点、文本和 edge，执行布局并一次提交。
- [ ] 导入前场景发生协作变化时重新校验输入，但不覆盖或挂接到已有 Mindmap。
- [ ] 读取失败、解析失败和协作变化导致的校验失败不产生半成品或空历史。

### 文本导出与往返

- [ ] 按逻辑顺序导出整图或选中子树，包含完整结构中的折叠后代。
- [ ] 三种文本格式保证支持范围内的文字、层级与顺序往返。
- [ ] 界面说明样式、布局配置、精确坐标和折叠状态不作为文本格式的完整保真承诺，完整场景继续使用 JSON。
- [ ] PNG/SVG 当前可见状态导出继续沿用 P04，不受文本导出完整层级规则影响。

## 界面验收

提供每种格式的有效三层样例、异常样例及带折叠状态的导图样例。

| 编号 | 操作 | 预期结果 |
| --- | --- | --- |
| P08-01 | 分别导入 Markdown、Outline、OPML 三层样例 | 解析通过后直接生成独立导图，结构和节点数量正确 |
| P08-02 | 在已有场景中连续导入同一份输入两次 | 两次结果都是独立导图，`graphId`、节点和 edge 身份不同，原有导图不变 |
| P08-03 | 从折叠导图导出三种格式，再重新导入 | 折叠后代没有丢失，文字、层级及兄弟顺序保持 |
| P08-04 | 导入错误文件、非法缩进或超限输入 | 明确提示问题，场景保持原样 |
| P08-05 | 导入失败后再执行一次正常导入并撤销 | 失败不改变场景；一次撤销完整移除该次导入 |
| P08-06 | 导入前由另一端修改或删除场景中的其他内容，再执行导入 | 不覆盖远端修改；只创建一张新的独立导图 |
| P08-07 | 导入后在另一端查看，保存重开并用大纲检查 | 协作、画布、大纲与持久化结果一致 |

## 自动化与回归

- [ ] 各格式解析、序列化、支持范围内往返及不同层级顺序测试。
- [ ] 非法输入、空输入、多根输入的提示和处理、规模限制与安全解析测试。
- [ ] 独立新导图的身份重建、原子历史与失败无副作用测试。
- [ ] 协作场景变化、配置继承、JSON 保存与 P04 图像导出回归。

## 本期不包含

不承诺文本格式完整保留 Excalidraw 的所有样式字段；不把 Markdown 内容渲染为可执行富文本。模板管理在 P09，AI 服务在 P11。

## 完成与交付

- [ ] 提供三种格式的可下载有效/异常样例、导入入口和导出往返验收步骤。
- [ ] 支持范围与格式损失在界面明确，导入可一次撤销。
- [ ] 本页及 [共用完成门槛](00-mindmap-overview.md) 通过，用户确认后进入 P10。
