# 第 10 期：移动端与大图性能

- 期次：P10。
- 状态：界面、真实触屏、双端协作及性能采集已获用户确认；待实测报告补录和剩余代码任务收尾。
- 里程碑：触摸设备和大规模导图的可用性闭环。
- 总体约束与完成门槛：[分期总览](00-mindmap-overview.md)。

## 依赖关系

- **前置验收**：[P08 文本格式导入导出](08-mindmap-import-and-export.md) 须先通过用户界面审查；P09 模板与主题暂缓，不作为本期前置条件。
- **主要能力依赖**：[P02](02-mindmap-tree-operations.md) 的无键盘树操作；[P03](03-mindmap-drag-and-move.md) 的会话与取消；[P04](04-mindmap-copy-and-integration.md) 的原生集成；[P05](05-mindmap-style-and-layout.md) 的方向适配；[P07](07-mindmap-search-and-outline.md) 的大图定位。
- **外部依赖**：可用手机、平板，以及固定的桌面性能验收设备；协作回归沿用 P06。
- **后续承接**：[P11 AI](11-mindmap-ai.md) 基于累计版本接入，并沿用本期的移动界面和规模保护。

## 用户可见目标

用户不依赖实体键盘即可在手机和平板上完成核心 Mindmap 编辑；中大型导图的拖拽、折叠、搜索和编辑保持可用。

## 进度核对（2026-09-25）

本期已接入手机节点操作栏、触摸选中后拖拽、根节点整图移动、第二触点与 `pointercancel` 取消，并在拖拽会话中复用目标图索引。元素、移动、拖拽、树操作和样例恢复的定向测试共 119 项通过。

用户确认触屏同级排序、多方向布局下的命中、软键盘与屏幕旋转均已验证；固定设备性能数据、真实触屏、双端协作和 P10 界面验收也已完成。P10-01 至 P10-08 的界面结果按用户确认记为通过。

四份 500/2000 节点样例及生成脚本已加入 Git 交付。仓库内尚无固定设备参数、优化前后 FPS 数值和实测步骤，交付报告仍需补录，不能代填采集结果。代码侧挂接排序仍有递归遍历，整图移动仍在每次指针移动时遍历场景；这两项继续列为实现清单中的待收尾项。

## 实施任务

### 移动界面与手势

- [x] 在手机和平板提供工具入口，以及创建、编辑、添加、折叠和上下文操作控件；界面验收已获用户确认。
- [x] 提供提升、拆图、删除节点、删除子树等无键盘操作方式，不依赖桌面 hover 才能发现功能；界面验收已获用户确认。
- [x] 支持触摸子树拖拽、目标提示、同级排序、取消和根节点整图移动；真实触屏验收已获用户确认。
- [x] 明确区分节点操作、画布平移与双指缩放；进入双指手势或收到 `pointercancel` 时不能误提交旧拖拽目标；真实触屏验收已获用户确认。
- [x] 多方向布局下的控制点、命中范围和预览适合触屏操作；真实触屏验收已获用户确认。
- [x] 处理软键盘遮挡、输入焦点、屏幕旋转与工具切换；文字输入不触发结构快捷操作；真实触屏验收已获用户确认。
- [x] 复用 owner document/window 与现有设备布局机制，不引入依赖全局窗口的专用分支。

### 大图计算与渲染

- [ ] 优化图索引与目标查找的复用、受影响图的布局、动画帧节流和渲染更新。
- [ ] 不为每次 pointermove 重建完整 React 界面或无关导图的结构。
- [ ] 保持深树遍历不依赖递归调用栈，折叠数据完整保留；挂接排序仍有递归遍历。
- [x] 提供 500 个可见节点的中型样例，以及 2000 个总节点的宽树、深树和折叠样例；四份样例及生成脚本已加入 Git 交付。
- [x] 进入本期时固定设备、浏览器、窗口、缩放和样例，记录优化前后同条件数据；用户确认已完成采集，具体记录待补入本页。
- [ ] 桌面 500 可见节点的持续拖拽预览以至少 30 FPS 为目标；2000 总节点样例可正常打开、折叠、搜索与编辑，无崩溃和持续无响应。交付报告列明实测结果，未达到的目标不能隐去。

## 界面验收

真实触屏验收与性能采集分别执行；浏览器桌面触摸模拟只作补充。

P10-01 至 P10-08 已获用户界面验收确认；设备及性能实测数据待补入交付报告。

| 编号 | 操作 | 预期结果 |
| --- | --- | --- |
| P10-01 | 在手机上创建、重命名、添加子节点并折叠 | 无需键盘快捷键和鼠标悬停，控件可发现并可点中 |
| P10-02 | 在平板上挂接子树、调整顺序并移动根节点 | 与桌面结构语义一致，预览清楚，整图移动不拆散 |
| P10-03 | 拖动中加入第二根手指缩放，再结束手势 | 不向之前目标误提交，预览及时清理 |
| P10-04 | 用菜单执行提升、拆图、删除子树、撤销重做 | 与桌面命令一致，一次撤销恢复完整操作 |
| P10-05 | 打开软键盘输入长标题，旋转屏幕并切换工具 | 焦点与布局正确，没有残留拖拽或额外节点 |
| P10-06 | 在固定设备打开 500 可见节点样例并持续拖动 | 预览持续响应，提供至少 30 FPS 目标的实测结果 |
| P10-07 | 打开 2000 总节点宽树/深树，折叠、搜索隐藏节点并编辑 | 无栈溢出、数据丢失或持续无响应，定位及编辑正确 |
| P10-08 | 触屏与桌面两端协作修改同一场景 | 两端收敛，移动端临时预览不泄露 |

## 自动化与回归

- [ ] 覆盖 pointer 类型、手势切换、指针取消、工具切换和屏幕变化。
- [ ] 覆盖大图索引、布局、折叠与深树安全性，记录关键性能采集结果。
- [x] 覆盖控件可访问标签和非 hover 入口的交互测试；现有测试覆盖手机节点操作栏，平板入口另见上方未完成任务。
- [ ] 回归所有桌面手势、混选扩展、复制拖拽和协作取消路径。
- [ ] 优化不能改变排序、布局确定性、历史次数或导出结果。

## 完成与交付

- [ ] 提供手机、平板实测步骤及设备信息，不能仅凭模拟器宣称触摸完成；实测已获用户确认，步骤及设备信息待补录。
- [ ] 提供可从界面加载的 500/2000 节点样例与同条件性能对照结果；样例已加入 Git，性能数值待补录。
- [ ] 本页及 [共用完成门槛](00-mindmap-overview.md) 通过，用户确认后进入 P11。
